---
name: Add a new Pipelines setup

prompt:
  project: Project Name
  password: admin

actions:
  - cd /data/provision && ansible-playbook -i host add-pipelines.yml --vault-password-file vault.key -u wcladmin -b -e "pipelines_name={{ project }}" -e "pipelines_admin_pass={{ password }}"
